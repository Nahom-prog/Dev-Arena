/**
 * React Question Pack
 * 4 Difficulties: Easy (20), Mid (20), Hard (20), Very Hard (20)
 * Total: 80 Questions
 * Correct answers are evenly and unpredictably distributed across A, B, C, and D.
 */

export const reactChallenges = [
  {
    title: "React - Practical Fundamentals",
    description: "JSX syntax, props vs state, event handling, list rendering with keys, and controlled form inputs.",
    difficulty: "easy",
    tags: ["React", "Frontend"],
    timeLimitMinutes: 15,
    questions: [
      {
        question: "Why must React components return a single root element (or Fragment `<>...</>`) in JSX?",
        codeSnippet: "return (\n  <>\n    <h1>Title</h1>\n    <p>Subtitle</p>\n  </>\n);",
        language: "javascript",
        options: [
          "JSX compiles to `React.createElement()` calls, which must return a single JavaScript object representation",
          "Browsers cannot render more than one HTML tag at a time",
          "Fragments prevent CSS from leaking into the DOM",
          "React Fiber limits DOM nodes to 1 per component",
        ],
        correctAnswer: "JSX compiles to `React.createElement()` calls, which must return a single JavaScript object representation",
        explanation: "Every JSX expression translates to a `React.createElement(type, props, ...children)` function call. A function can only return a single expression/object.",
      },
      {
        question: "What is the primary difference between `props` and `state` in a React component?",
        options: [
          "`props` are passed into a component from outside and are read-only; `state` is managed internally by the component and can change",
          "`state` is passed from parent to child, while `props` are created internally",
          "`props` can be mutated directly by child components",
          "There is no difference; they are aliases for the same object",
        ],
        correctAnswer: "`props` are passed into a component from outside and are read-only; `state` is managed internally by the component and can change",
        explanation: "Props represent configuration and data passed down from parent components (immutable to the receiver). State represents local, reactive memory that triggers re-renders when updated.",
      },
      {
        question: "How do you pass a string prop named `title` to a child component in JSX?",
        codeSnippet: "<Header title=\"Dashboard Overview\" />",
        language: "javascript",
        options: [
          "<Header title=\"Dashboard Overview\" />",
          "<Header props.title=\"Dashboard Overview\" />",
          "<Header (title)=\"Dashboard Overview\" />",
          "<Header :title=\"Dashboard Overview\" />",
        ],
        correctAnswer: "<Header title=\"Dashboard Overview\" />",
        explanation: "Props are passed as attributes on JSX elements: `<Component propName=\"value\" />` or `<Component propName={expression} />`.",
      },
      {
        question: "What happens if you omit the `key` prop when rendering a dynamic list in React?",
        codeSnippet: "{items.map(item => (\n  <li>{item.name}</li> // Missing key\n))}",
        language: "javascript",
        options: [
          "The component throws a fatal compile error and crashes the app",
          "React issues a console warning and may incorrectly update or reorder DOM elements during list reconciliation",
          "The list is rendered in reverse order",
          "CSS styles are stripped from the list items",
        ],
        correctAnswer: "React issues a console warning and may incorrectly update or reorder DOM elements during list reconciliation",
        explanation: "React logs a warning: `Each child in a list should have a unique 'key' prop`. Without keys, React relies on element indices, which can lead to UI and state mismatch bugs.",
      },
      {
        question: "Which hook initializes and updates state inside a functional React component?",
        codeSnippet: "const [count, setCount] = useState(0);",
        language: "javascript",
        options: [
          "useEffect",
          "useRef",
          "useState",
          "useReducer",
        ],
        correctAnswer: "useState",
        explanation: "`useState(initialState)` declares a state variable and provides an updater function to update that value and trigger a re-render.",
      },
      {
        question: "How do you write an inline click handler that invokes `handleClick` with an ID argument?",
        codeSnippet: "<button onClick={() => handleClick(item.id)}>Delete</button>",
        language: "javascript",
        options: [
          "<button onClick={handleClick(item.id)}>Delete</button>",
          "<button onClick={() => handleClick(item.id)}>Delete</button>",
          "<button onClick=\"handleClick(item.id)\">Delete</button>",
          "<button on-click={handleClick(item.id)}>Delete</button>",
        ],
        correctAnswer: "<button onClick={() => handleClick(item.id)}>Delete</button>",
        explanation: "Wrapping the invocation in an arrow function `() => handleClick(item.id)` ensures the function is called on user click, rather than immediately during render.",
      },
      {
        question: "What makes an `<input>` element a 'Controlled Component' in React?",
        codeSnippet: "<input value={text} onChange={e => setText(e.target.value)} />",
        language: "javascript",
        options: [
          "Its value is driven by React component state and updated through an event handler",
          "It uses a `ref` to read the DOM directly",
          "It cannot be submitted in HTML forms",
          "It is rendered inside an iframe",
        ],
        correctAnswer: "Its value is driven by React component state and updated through an event handler",
        explanation: "In a controlled component, the input value is bound to React state via `value={state}`, making React the single source of truth.",
      },
      {
        question: "How do you conditionally render an alert component only when `hasError` is truthy?",
        codeSnippet: "{hasError && <ErrorAlert message=\"Failed to load\" />}",
        language: "javascript",
        options: [
          "{hasError ? <ErrorAlert message=\"Failed to load\" /> : null}",
          "{hasError && <ErrorAlert message=\"Failed to load\" />}",
          "Both A and B are valid conditional rendering patterns in React",
          "Neither; `v-if` must be used in React",
        ],
        correctAnswer: "Both A and B are valid conditional rendering patterns in React",
        explanation: "Both ternary operators `hasError ? <Component /> : null` and short-circuit evaluation `hasError && <Component />` are standard React conditional rendering techniques.",
      },
      {
        question: "What is the second argument of `useState` used for?",
        codeSnippet: "const [active, setActive] = useState(false);",
        language: "javascript",
        options: [
          "The initial value of the state",
          "The updater function used to change state and trigger a component re-render",
          "A callback executed when the component unmounts",
          "The DOM node selector",
        ],
        correctAnswer: "The updater function used to change state and trigger a component re-render",
        explanation: "`useState` returns a 2-element tuple: the current state value, and a state setter function.",
      },
      {
        question: "What attribute replaces standard HTML `class` when applying CSS classes in JSX?",
        codeSnippet: "<div className=\"card-container\">Content</div>",
        language: "javascript",
        options: [
          "class",
          "className",
          "cssClass",
          "styleClass",
        ],
        correctAnswer: "className",
        explanation: "Because `class` is a reserved keyword in JavaScript, JSX uses `className` to specify CSS classes on DOM elements.",
      },
      {
        question: "What does the `children` prop represent in React?",
        codeSnippet: "function Modal({ children }) {\n  return <div className=\"modal-backdrop\">{children}</div>;\n}",
        language: "javascript",
        options: [
          "The nested elements passed between the opening and closing tags of a component: `<Modal><p>Hello</p></Modal>`",
          "An array of child component IDs in the database",
          "The DOM event target elements",
          "The sub-routes defined in React Router",
        ],
        correctAnswer: "The nested elements passed between the opening and closing tags of a component: `<Modal><p>Hello</p></Modal>`",
        explanation: "`props.children` allows components to compose and render arbitrary nested JSX passed between their opening and closing tags.",
      },
      {
        question: "What style format does the JSX `style` attribute accept?",
        codeSnippet: "<div style={{ backgroundColor: '#10b981', padding: '12px' }}>Box</div>",
        language: "javascript",
        options: [
          "A plain CSS string: `style=\"background-color: green;\"`",
          "A JavaScript object with camelCased property names: `style={{ backgroundColor: '#10b981' }}`",
          "An array of stylesheet URLs",
          "A Tailwind utility string",
        ],
        correctAnswer: "A JavaScript object with camelCased property names: `style={{ backgroundColor: '#10b981' }}`",
        explanation: "In React, inline styles are passed as JavaScript objects with camelCase keys (e.g. `backgroundColor` instead of `background-color`).",
      },
      {
        question: "Why should you NOT modify React state directly like `user.name = 'Alex'`?",
        codeSnippet: "const [user, setUser] = useState({ name: 'Nahom' });\n// Wrong: user.name = 'Alex';",
        language: "javascript",
        options: [
          "Direct mutation does not trigger a component re-render, leaving the UI stale and out of sync",
          "It throws a syntax error at compile time",
          "It deletes all other keys from the state object",
          "It breaks browser cookie storage",
        ],
        correctAnswer: "Direct mutation does not trigger a component re-render, leaving the UI stale and out of sync",
        explanation: "React relies on calling the state setter function (`setUser(...)`) to detect changes via shallow reference equality and schedule a re-render.",
      },
      {
        question: "What is a React Fragment `<React.Fragment>` or `<></>` used for?",
        options: [
          "Groups multiple JSX elements without adding an extra node to the real DOM",
          "Encapsulates CSS styles like a shadow DOM",
          "Optimizes images for responsive screens",
          "Caches API request data",
        ],
        correctAnswer: "Groups multiple JSX elements without adding an extra node to the real DOM",
        explanation: "Fragments let you group a list of children without adding extra `<div>` wrapper nodes to the rendered HTML DOM tree.",
      },
      {
        question: "When does an effect function run if passed an empty dependency array `[]`?",
        codeSnippet: "useEffect(() => {\n  console.log('Mounted');\n}, []);",
        language: "javascript",
        options: [
          "Only once after the initial render (mount) of the component",
          "Before every single render",
          "Only when the user clicks anywhere on the page",
          "Never runs",
        ],
        correctAnswer: "Only once after the initial render (mount) of the component",
        explanation: "Passing an empty array `[]` tells React that the effect doesn't depend on any values from props or state, so it executes only once when the component mounts.",
      },
      {
        question: "What is the naming convention for custom hooks in React?",
        options: [
          "Must begin with the lowercase word `use`, such as `useWindowSize` or `useAuth`",
          "Must end with `Hook`, such as `windowSizeHook`",
          "Must be capitalized, such as `GetUserData`",
          "Must be prefixed with `react_`",
        ],
        correctAnswer: "Must begin with the lowercase word `use`, such as `useWindowSize` or `useAuth`",
        explanation: "React's linter uses the `use` prefix convention (e.g. `useFetch`, `useAuth`) to identify hooks and enforce the Rules of Hooks.",
      },
      {
        question: "What does the `useRef` hook return?",
        codeSnippet: "const inputRef = useRef(null);",
        language: "javascript",
        options: [
          "A mutable ref object with a `.current` property that persists across re-renders without causing a re-render when mutated",
          "A state variable and an updater function",
          "A snapshot of the DOM window",
          "A memoized function reference",
        ],
        correctAnswer: "A mutable ref object with a `.current` property that persists across re-renders without causing a re-render when mutated",
        explanation: "`useRef` returns `{ current: initialValue }`. Mutating `.current` does not trigger a component re-render, making it ideal for DOM references or mutable instance variables.",
      },
      {
        question: "How do you access the native event object in a React event handler?",
        codeSnippet: "const handleChange = (event) => {\n  console.log(event.target.value);\n};",
        language: "javascript",
        options: [
          "Via the SyntheticEvent object passed automatically as the first parameter to the handler",
          "By reading `window.event`",
          "By injecting an event context provider",
          "Through `this.event`",
        ],
        correctAnswer: "Via the SyntheticEvent object passed automatically as the first parameter to the handler",
        explanation: "React wraps native browser events in a cross-browser `SyntheticEvent` wrapper and passes it as the first parameter to event callbacks.",
      },
      {
        question: "What does `React.StrictMode` do during development in modern React?",
        options: [
          "Intentionally double-invokes component renders and effects in dev mode to help identify unexpected side effects",
          "Disables all CSS transitions",
          "Enforces TypeScript compilation on vanilla JS",
          "Blocks network calls to unencrypted HTTP endpoints",
        ],
        correctAnswer: "Intentionally double-invokes component renders and effects in dev mode to help identify unexpected side effects",
        explanation: "In development, `StrictMode` runs component bodies and effects twice to ensure that components are pure and effect cleanups properly reset state.",
      },
      {
        question: "What does calling `event.preventDefault()` inside a form submit handler achieve?",
        codeSnippet: "function handleSubmit(e) {\n  e.preventDefault();\n  // Submit via fetch\n}",
        language: "javascript",
        options: [
          "Prevents the default browser behavior of refreshing the entire page and submitting a GET/POST request",
          "Clears all input values in the form",
          "Stops event bubbling up the DOM hierarchy",
          "Disables the submit button",
        ],
        correctAnswer: "Prevents the default browser behavior of refreshing the entire page and submitting a GET/POST request",
        explanation: "`e.preventDefault()` suppresses the standard browser page reload on form submissions, allowing Single Page Apps to handle submission asynchronously via JavaScript.",
      },
    ],
  },
  {
    title: "React - Real-World Gotchas & Patterns",
    description: "Functional updaters, useEffect dependency traps, custom hook abstractions, memoization, and context.",
    difficulty: "mid",
    tags: ["React", "Frontend"],
    timeLimitMinutes: 18,
    questions: [
      {
        question: "Why should you use the functional updater form `setCount(prev => prev + 1)` when updates depend on prior state?",
        codeSnippet: "const handleTripleClick = () => {\n  setCount(c => c + 1);\n  setCount(c => c + 1);\n  setCount(c => c + 1);\n};",
        language: "javascript",
        options: [
          "It guarantees each update receives the most up-to-date queued state value, avoiding stale closure overwrites",
          "It forces React to re-render three times synchronously",
          "It stores the value in localStorage automatically",
          "It converts the counter into a web worker thread",
        ],
        correctAnswer: "It guarantees each update receives the most up-to-date queued state value, avoiding stale closure overwrites",
        explanation: "If you call `setCount(count + 1)` three times in the same tick, all three calls read the stale snapshot of `count`. The functional updater `(prev => prev + 1)` operates on the queue.",
      },
      {
        question: "What is the primary role of `useMemo` in a React component?",
        codeSnippet: "const expensiveList = useMemo(() => {\n  return computeHeavyCalculation(items);\n}, [items]);",
        language: "javascript",
        options: [
          "To cache and memoize the computed result of an expensive calculation between renders unless dependencies change",
          "To cache DOM element references",
          "To memoize callback functions across renders",
          "To prevent parent components from re-rendering",
        ],
        correctAnswer: "To cache and memoize the computed result of an expensive calculation between renders unless dependencies change",
        explanation: "`useMemo(calculateValue, dependencies)` caches the calculated value between renders until one of its dependencies changes.",
      },
      {
        question: "What is the difference between `useCallback` and `useMemo`?",
        options: [
          "`useCallback(fn, deps)` caches a function definition itself; `useMemo(() => fn(), deps)` caches the returned value of a function",
          "`useCallback` can only be used in class components",
          "`useMemo` is asynchronous, while `useCallback` is synchronous",
          "There is no difference; they are exact aliases",
        ],
        correctAnswer: "`useCallback(fn, deps)` caches a function definition itself; `useMemo(() => fn(), deps)` caches the returned value of a function",
        explanation: "`useCallback(fn, deps)` is equivalent to `useMemo(() => fn, deps)`. It memoizes the function reference to prevent child re-renders.",
      },
      {
        question: "What causes an infinite loop when updating state inside `useEffect` without proper dependencies?",
        codeSnippet: "useEffect(() => {\n  setCount(count + 1);\n}); // Missing dependency array",
        language: "javascript",
        options: [
          "The effect runs on every render, sets state, triggers another render, which triggers the effect again infinitely",
          "React Fiber crashes when dependencies are empty",
          "The browser runs out of stack frames synchronously",
          "Memory allocation fails on the GPU",
        ],
        correctAnswer: "The effect runs on every render, sets state, triggers another render, which triggers the effect again infinitely",
        explanation: "Without a dependency array, `useEffect` runs after every single render. Updating state causes a re-render, creating an infinite render loop.",
      },
      {
        question: "How does React Context help prevent 'prop drilling' in component trees?",
        codeSnippet: "const ThemeContext = createContext('dark');\n// Any deep descendant can consume without intermediate prop forwarding:\nconst theme = useContext(ThemeContext);",
        language: "javascript",
        options: [
          "It provides a way to share values through the component tree without having to pass props down manually at every level",
          "It compresses prop data in the browser cache",
          "It replaces the need for React Router",
          "It eliminates re-rendering across the entire app",
        ],
        correctAnswer: "It provides a way to share values through the component tree without having to pass props down manually at every level",
        explanation: "React Context allows a parent component to broadcast data to all descendants in the tree below it, regardless of depth.",
      },
      {
        question: "What common performance pitfall occurs when passing newly created object literals directly to Context Provider values?",
        codeSnippet: "<AuthContext.Provider value={{ user, token }}>",
        language: "javascript",
        options: [
          "Every render of the Provider creates a new object reference `{ user, token }`, triggering re-renders in all consumers even if values haven't changed",
          "Context cannot accept objects",
          "It throws a React warning about unmemoized contexts",
          "It causes a memory leak on the server",
        ],
        correctAnswer: "Every render of the Provider creates a new object reference `{ user, token }`, triggering re-renders in all consumers even if values haven't changed",
        explanation: "Because `{ user, token }` creates a new object reference on every render, React sees a new context value and re-renders every component consuming `AuthContext`. Wrapping it in `useMemo` fixes this.",
      },
      {
        question: "When should you use `useLayoutEffect` instead of `useEffect`?",
        codeSnippet: "useLayoutEffect(() => {\n  const rect = ref.current.getBoundingClientRect();\n  setHeight(rect.height);\n}, []);",
        language: "javascript",
        options: [
          "When you need to measure DOM layout synchronously before the browser paints to prevent visual flickering",
          "When performing network fetch requests",
          "When setting timers and intervals",
          "When writing server components",
        ],
        correctAnswer: "When you need to measure DOM layout synchronously before the browser paints to prevent visual flickering",
        explanation: "`useLayoutEffect` fires synchronously after all DOM mutations and before the browser repaints screen pixels, preventing visible layout flickers.",
      },
      {
        question: "What does wrapping a child component in `React.memo()` do?",
        codeSnippet: "const MemoizedCard = React.memo(UserCard);",
        language: "javascript",
        options: [
          "Skips re-rendering the component if its new props are shallowly equal to its previous props",
          "Stores the component's HTML in localStorage",
          "Prevents the component from using state",
          "Makes all state updates in the component synchronous",
        ],
        correctAnswer: "Skips re-rendering the component if its new props are shallowly equal to its previous props",
        explanation: "`React.memo()` is a higher order component that memoizes the rendered output, skipping re-rendering if props have not changed by shallow comparison.",
      },
      {
        question: "Why will `React.memo(Child)` still re-render if the parent passes an inline arrow function prop?",
        codeSnippet: "<MemoizedChild onSave={() => handleSave(id)} />",
        language: "javascript",
        options: [
          "A brand new function reference is created on every parent render, failing shallow equality comparison `prevProps.onSave !== nextProps.onSave`",
          "`React.memo` only works on primitives, not functions",
          "Arrow functions are not supported in JSX props",
          "Child components cannot receive functions as props",
        ],
        correctAnswer: "A brand new function reference is created on every parent render, failing shallow equality comparison `prevProps.onSave !== nextProps.onSave`",
        explanation: "Inline arrow functions receive a new memory reference on each render. Wrapping the handler in `useCallback` maintains reference stability so `React.memo` succeeds.",
      },
      {
        question: "What is the purpose of the `useReducer` hook in React?",
        codeSnippet: "const [state, dispatch] = useReducer(reducer, initialState);",
        language: "javascript",
        options: [
          "Managing complex state logic where next state depends on previous state and multiple sub-values through action dispatching",
          "Compressing images before uploading",
          "Reducing bundle size during Vite build",
          "Handling WebSocket reconnections",
        ],
        correctAnswer: "Managing complex state logic where next state depends on previous state and multiple sub-values through action dispatching",
        explanation: "`useReducer` provides a Redux-like pattern for managing intricate state transitions via `dispatch({ type, payload })` and pure reducer functions.",
      },
      {
        question: "What is a 'stale closure' in a React `useEffect` or callback hook?",
        codeSnippet: "useEffect(() => {\n  const interval = setInterval(() => {\n    console.log(count); // Always prints initial value!\n  }, 1000);\n  return () => clearInterval(interval);\n}, []); // Missing count in deps",
        language: "javascript",
        options: [
          "The callback captured variables from an earlier render and never sees updated state because it wasn't recreated",
          "A closure that has been deleted from memory",
          "An error thrown when closures exceed 10MB",
          "When an effect runs after the component unmounts",
        ],
        correctAnswer: "The callback captured variables from an earlier render and never sees updated state because it wasn't recreated",
        explanation: "When dependencies are omitted, functions retain closed-over references to the state variables from the render in which they were created, never seeing newer values.",
      },
      {
        question: "What is the role of `forwardRef` in React component libraries?",
        codeSnippet: "const CustomInput = forwardRef((props, ref) => (\n  <input ref={ref} {...props} />\n));",
        language: "javascript",
        options: [
          "Allows a parent component to pass a `ref` through a custom component down to one of its underlying DOM nodes",
          "Forwards API calls to a backend server",
          "Redirects navigation to a new URL",
          "Automatically animates DOM elements",
        ],
        correctAnswer: "Allows a parent component to pass a `ref` through a custom component down to one of its underlying DOM nodes",
        explanation: "Standard function components do not accept a `ref` prop. `forwardRef` intercepts the `ref` and passes it as a second argument to the component render function.",
      },
      {
        question: "What does the `useImperativeHandle` hook customize?",
        codeSnippet: "useImperativeHandle(ref, () => ({\n  focus() { inputRef.current.focus(); },\n  reset() { inputRef.current.value = ''; }\n}));",
        language: "javascript",
        options: [
          "Customizes the instance value and methods exposed to parent components when using `ref`",
          "Intercepts HTTP requests",
          "Controls CSS animation frames",
          "Creates imperative SQL queries",
        ],
        correctAnswer: "Customizes the instance value and methods exposed to parent components when using `ref`",
        explanation: "`useImperativeHandle(ref, createHandle)` allows child components to expose specific imperative methods to parents instead of giving unrestricted access to the raw DOM node.",
      },
      {
        question: "What does the second argument to `React.memo(Component, arePropsEqual)` allow?",
        codeSnippet: "export default React.memo(MyComponent, (prevProps, nextProps) => {\n  return prevProps.id === nextProps.id;\n});",
        language: "javascript",
        options: [
          "A custom comparison function to determine whether to skip re-rendering (returning `true` skips render)",
          "An error boundary fallback component",
          "A callback that runs after unmounting",
          "The maximum cache duration in milliseconds",
        ],
        correctAnswer: "A custom comparison function to determine whether to skip re-rendering (returning `true` skips render)",
        explanation: "The custom comparison function receives `prevProps` and `nextProps`. Returning `true` tells React the props are equivalent and no re-render is necessary.",
      },
      {
        question: "Why should side effects (like API fetching or direct DOM manipulation) NEVER be placed directly in the component body?",
        codeSnippet: "function BadComponent() {\n  fetch('/api/track'); // Side effect in render!\n  return <div>Bad</div>;\n}",
        language: "javascript",
        options: [
          "Render functions must be pure calculations with no side effects; side effects in render execute repeatedly on every render pass and during concurrency checks",
          "React throws a compile error if `fetch` is found in the body",
          "Direct fetch calls cause browser tabs to crash",
          "It converts the component to a Server Action",
        ],
        correctAnswer: "Render functions must be pure calculations with no side effects; side effects in render execute repeatedly on every render pass and during concurrency checks",
        explanation: "React components must remain pure functions of their props and state. Side effects must be placed in event handlers or `useEffect` hooks.",
      },
      {
        question: "What is the purpose of the key prop when applied to a component rather than a list item?",
        codeSnippet: "<UserProfile key={userId} userId={userId} />",
        language: "javascript",
        options: [
          "Changing the `key` instructs React to completely unmount the old component and remount a fresh instance with reset state",
          "It accelerates CSS styling calculations",
          "It binds the component to an index in IndexedDB",
          "It makes the component draggable",
        ],
        correctAnswer: "Changing the `key` instructs React to completely unmount the old component and remount a fresh instance with reset state",
        explanation: "Giving a component a unique `key` (e.g. `key={userId}`) resets its state completely when the key changes, cleanly destroying the previous instance and mounting a new one.",
      },
      {
        question: "How do you avoid recreating expensive initial state in `useState` on every render?",
        codeSnippet: "const [data, setData] = useState(() => calculateHeavyInitialData());",
        language: "javascript",
        options: [
          "Pass an initializer function `() => compute()`: React will execute it only once during initial mount",
          "Wrap the state variable in `useRef`",
          "Call `useState` inside a `useMemo`",
          "Store the value in a global window variable",
        ],
        correctAnswer: "Pass an initializer function `() => compute()`: React will execute it only once during initial mount",
        explanation: "Passing an initializer function (`lazy initial state`) ensures the expensive calculation runs only during the initial mount, not on subsequent component renders.",
      },
      {
        question: "What does `useId` hook generate in React 18+?",
        codeSnippet: "const id = useId();\nreturn <label htmlFor={id}>Email: <input id={id} /></label>;",
        language: "javascript",
        options: [
          "A unique, stable ID across server-side rendering and client hydration to avoid hydration mismatches for accessibility attributes",
          "A cryptographic random UUID v4 string",
          "A database primary key",
          "A timestamp in milliseconds",
        ],
        correctAnswer: "A unique, stable ID across server-side rendering and client hydration to avoid hydration mismatches for accessibility attributes",
        explanation: "`useId()` produces unique identifiers that match identically between SSR HTML and client hydration, solving accessibility linking issues without ID collisions.",
      },
      {
        question: "Why can't React hooks be called inside loops, conditions, or nested functions?",
        options: [
          "React relies on the exact order in which hooks are called across renders to match hook state with its internal linked list",
          "JavaScript engines disallow function calls inside if blocks",
          "Hooks require global scope evaluation",
          "It confuses the Babel JSX transpiler",
        ],
        correctAnswer: "React relies on the exact order in which hooks are called across renders to match hook state with its internal linked list",
        explanation: "Hooks are stored in an ordered linked list attached to the component's Fiber node. Conditional execution alters the call order, causing state mismatches across renders.",
      },
      {
        question: "What does the `useDebugValue` hook do?",
        codeSnippet: "function useOnlineStatus() {\n  const isOnline = ...;\n  useDebugValue(isOnline ? 'Online' : 'Offline');\n  return isOnline;\n}",
        language: "javascript",
        options: [
          "Displays a custom label for custom hooks inside React Developer Tools",
          "Prints debug logs to the Node.js terminal",
          "Triggers a debugger breakpoint in Chrome DevTools",
          "Reports errors to Sentry",
        ],
        correctAnswer: "Displays a custom label for custom hooks inside React Developer Tools",
        explanation: "`useDebugValue` is used inside custom hooks to display a friendly debug label in React DevTools inspectors.",
      },
    ],
  },
  {
    title: "React - Deep Mechanics & Internals",
    description: "Fiber reconciliation, work-in-progress trees, automatic batching, Suspense boundaries, and portal mechanics.",
    difficulty: "hard",
    tags: ["React", "Frontend"],
    timeLimitMinutes: 20,
    questions: [
      {
        question: "What is a 'Fiber' in the React Fiber architecture?",
        options: [
          "A lightweight JavaScript object representing a unit of work and a node in the virtual DOM component tree",
          "A WebAssembly thread running parallel computations",
          "A CSS layout engine inside modern Chrome",
          "An HTTP/2 stream pipeline",
        ],
        correctAnswer: "A lightweight JavaScript object representing a unit of work and a node in the virtual DOM component tree",
        explanation: "A Fiber is an internal data structure that represents a component and its state, props, and DOM relationships. It allows React to split rendering work into chunks and pause/resume work.",
      },
      {
        question: "What are the two primary phases of the React Fiber reconciliation cycle?",
        options: [
          "The Render phase (reconciliation, asynchronous and interruptible) and the Commit phase (DOM updates, synchronous)",
          "The Parse phase and the Compile phase",
          "The Mount phase and the Unload phase",
          "The Server phase and the Browser phase",
        ],
        correctAnswer: "The Render phase (reconciliation, asynchronous and interruptible) and the Commit phase (DOM updates, synchronous)",
        explanation: "During the Render phase, React builds the work-in-progress Fiber tree (which can be paused or aborted). During the Commit phase, React applies changes to the real DOM synchronously.",
      },
      {
        question: "What is 'double buffering' in React Fiber?",
        options: [
          "React maintains two Fiber trees: the `current` tree (rendered on screen) and the `workInProgress` tree (being computed offscreen)",
          "React renders components onto two HTML canvases simultaneously",
          "React keeps two copies of state in localStorage",
          "React pre-renders pages two routes ahead",
        ],
        correctAnswer: "React maintains two Fiber trees: the `current` tree (rendered on screen) and the `workInProgress` tree (being computed offscreen)",
        explanation: "Double buffering allows React to construct the new UI state in a `workInProgress` tree without mutating the currently visible tree. Once ready, React swaps the pointer in a single commit step.",
      },
      {
        question: "How does React 18 automatic batching improve on React 17 state batching?",
        codeSnippet: "setTimeout(() => {\n  setCount(c => c + 1);\n  setFlag(f => !f);\n  // React 18 batches this into 1 render; React 17 rendered twice\n}, 100);",
        language: "javascript",
        options: [
          "Batching now applies universally across timeouts, promises, and native event listeners, not just synthetic React event handlers",
          "It runs all state updates in Web Workers",
          "It eliminates the need for `setCount` updater functions",
          "It batches updates across different browser tabs",
        ],
        correctAnswer: "Batching now applies universally across timeouts, promises, and native event listeners, not just synthetic React event handlers",
        explanation: "React 17 only batched updates inside React event handlers. React 18's createRoot batches all updates regardless of where they originate (promises, setTimeout, native listeners).",
      },
      {
        question: "How do you intentionally opt out of automatic batching to force a synchronous DOM update in React 18?",
        codeSnippet: "import { flushSync } from 'react-dom';\n\nflushSync(() => {\n  setCount(c => c + 1);\n});",
        language: "javascript",
        options: [
          "Wrap the state update in `flushSync(() => { ... })`",
          "Pass `{ sync: true }` to `setCount`",
          "Invoke `setImmediate(setCount)`",
          "Use `useLayoutEffect` instead",
        ],
        correctAnswer: "Wrap the state update in `flushSync(() => { ... })`",
        explanation: "`flushSync` forces React to flush any pending work inside the callback synchronously to the DOM immediately.",
      },
      {
        question: "What does an Error Boundary catch in a React component tree?",
        codeSnippet: "class ErrorBoundary extends React.Component {\n  static getDerivedStateFromError(error) { return { hasError: true }; }\n  componentDidCatch(error, info) { logError(error, info); }\n}",
        language: "javascript",
        options: [
          "Errors during rendering, in lifecycle methods, and in constructors of the whole tree below them",
          "Asynchronous errors inside `setTimeout` and promise handlers",
          "Errors inside event handlers (like onClick)",
          "Server-side compilation syntax errors",
        ],
        correctAnswer: "Errors during rendering, in lifecycle methods, and in constructors of the whole tree below them",
        explanation: "Error boundaries catch errors during rendering and in lifecycle hooks/constructors of children. They do not catch errors in event handlers (which need try/catch) or async code.",
      },
      {
        question: "What is the purpose of `ReactDOM.createPortal()`?",
        codeSnippet: "ReactDOM.createPortal(\n  <div className=\"modal-overlay\">Modal Content</div>,\n  document.getElementById('portal-root')\n);",
        language: "javascript",
        options: [
          "Renders children into a different part of the DOM tree outside the parent component's DOM hierarchy while preserving React event bubbling",
          "Opens a new browser popup window",
          "Creates an iframe sandbox",
          "Injects CSS stylesheets into the head tag",
        ],
        correctAnswer: "Renders children into a different part of the DOM tree outside the parent component's DOM hierarchy while preserving React event bubbling",
        explanation: "Portals let you render elements (like modals and tooltips) into a top-level DOM node (e.g. `document.body`) while maintaining normal React tree event propagation and context access.",
      },
      {
        question: "How does `<Suspense>` coordinate async component loading in React?",
        codeSnippet: "<Suspense fallback={<LoadingSpinner />}>\n  <LazyProfileFeed />\n</Suspense>",
        language: "javascript",
        options: [
          "Catches thrown promises from child components during render and displays the `fallback` UI until the promise resolves",
          "Runs an async while-loop in JavaScript until the data is loaded",
          "Downloads the component using a Web Worker",
          "Blocks the main browser thread until assets load",
        ],
        correctAnswer: "Catches thrown promises from child components during render and displays the `fallback` UI until the promise resolves",
        explanation: "When a Suspense-enabled component needs async data, it throws a Promise. The enclosing Suspense boundary catches it and shows the fallback UI until it resolves.",
      },
      {
        question: "What does `React.lazy()` require when dynamically importing components?",
        codeSnippet: "const Dashboard = React.lazy(() => import('./Dashboard'));",
        language: "javascript",
        options: [
          "The imported module must export the component as its `default` export, and it must be rendered inside a `<Suspense>` boundary",
          "The component must be written as a class component",
          "The import path must use `.jsx` file extensions",
          "The component must not use props",
        ],
        correctAnswer: "The imported module must export the component as its `default` export, and it must be rendered inside a `<Suspense>` boundary",
        explanation: "`React.lazy()` expects a dynamic `import()` call resolving to a module with a `default` export, and requires a `<Suspense>` wrapper to show a loading state during chunk download.",
      },
      {
        question: "Why does mutating an element's `children` array directly inside a component lead to reconciliation bugs?",
        options: [
          "React elements are immutable frozen objects (`Object.freeze`); mutations violate Fiber identity tracking and fail shallow checks",
          "Mutating arrays causes garbage collection crashes",
          "React components can only accept up to 3 children",
          "Vite strips children arrays during build",
        ],
        correctAnswer: "React elements are immutable frozen objects (`Object.freeze`); mutations violate Fiber identity tracking and fail shallow checks",
        explanation: "React elements are immutable descriptions of UI. Modifying children directly corrupts React's reconciliation comparison between the previous and next Fiber trees.",
      },
      {
        question: "What is the difference between mounting, updating, and unmounting in React Fiber lifecycle?",
        options: [
          "Mounting creates the Fiber and DOM node; Updating computes diffs and updates existing nodes; Unmounting removes nodes and invokes cleanups",
          "Mounting happens on the server; Updating happens in dev mode; Unmounting is for mobile devices",
          "All three occur simultaneously on every user click",
          "Mounting and updating are identical in React 18",
        ],
        correctAnswer: "Mounting creates the Fiber and DOM node; Updating computes diffs and updates existing nodes; Unmounting removes nodes and invokes cleanups",
        explanation: "Mounting inserts a component into the DOM. Updating processes new props or state changes. Unmounting cleans up resources and detaches the DOM node.",
      },
      {
        question: "What does the `useTransition` hook do in React 18+?",
        codeSnippet: "const [isPending, startTransition] = useTransition();\n\nstartTransition(() => {\n  setFilterQuery(input);\n});",
        language: "javascript",
        options: [
          "Marks state updates inside `startTransition` as non-urgent transitions that can be interrupted by urgent updates (like keystrokes)",
          "Applies CSS transition animations between pages",
          "Delays execution by 500ms using setTimeout",
          "Transitions the app into offline mode",
        ],
        correctAnswer: "Marks state updates inside `startTransition` as non-urgent transitions that can be interrupted by urgent updates (like keystrokes)",
        explanation: "`useTransition` allows developers to prioritize urgent updates (typing in an input) over heavy background renders (re-filtering a 10,000-row list), keeping the UI responsive.",
      },
      {
        question: "How does `useDeferredValue` differ from `useTransition`?",
        codeSnippet: "const deferredQuery = useDeferredValue(query);",
        language: "javascript",
        options: [
          "`useDeferredValue` accepts a value and defers updating it until higher-priority renders finish; `useTransition` wraps the state-updating function itself",
          "`useDeferredValue` uses localStorage",
          "`useDeferredValue` can only be used with numbers",
          "There is no difference; they are aliases",
        ],
        correctAnswer: "`useDeferredValue` accepts a value and defers updating it until higher-priority renders finish; `useTransition` wraps the state-updating function itself",
        explanation: "`useTransition` is used when you have access to the state setter (`startTransition(setVal)`). `useDeferredValue` is used when you only receive a prop or value from above.",
      },
      {
        question: "What is the purpose of `useSyncExternalStore` introduced in React 18?",
        codeSnippet: "const state = useSyncExternalStore(store.subscribe, store.getSnapshot);",
        language: "javascript",
        options: [
          "Allows third-party state libraries (Redux, Zustand) to subscribe to external stores safely during concurrent rendering without 'tearing'",
          "Synchronizes state with MongoDB databases",
          "Stores data in WebSQL",
          "Connects React to GraphQL subscriptions",
        ],
        correctAnswer: "Allows third-party state libraries (Redux, Zustand) to subscribe to external stores safely during concurrent rendering without 'tearing'",
        explanation: "`useSyncExternalStore` prevents 'tearing' (visual inconsistencies caused by an external store changing while React concurrently renders the tree) by enforcing synchronous store reads.",
      },
      {
        question: "What is 'tearing' in concurrent UI rendering?",
        options: [
          "A visual glitch where different components on screen display conflicting values for the same piece of data because the store updated midway through a concurrent render",
          "A CSS layout overflow bug",
          "A memory leak in HTML5 Canvas",
          "When a screen tear happens due to monitor refresh rates",
        ],
        correctAnswer: "A visual glitch where different components on screen display conflicting values for the same piece of data because the store updated midway through a concurrent render",
        explanation: "Tearing occurs when a concurrent render yields, an external store mutates, and subsequent components in the same render pass read the newer value, creating inconsistent UI.",
      },
      {
        question: "What does the `useInsertionEffect` hook do?",
        codeSnippet: "useInsertionEffect(() => {\n  const style = insertRule('.btn { color: red }');\n  return () => removeRule(style);\n}, []);",
        language: "javascript",
        options: [
          "Fires before all DOM mutations, specifically designed for CSS-in-JS libraries to inject style tags before layout effects read layout",
          "Inserts components into the DOM tree",
          "Handles SQL INSERT statements",
          "Inserts elements into an array state",
        ],
        correctAnswer: "Fires before all DOM mutations, specifically designed for CSS-in-JS libraries to inject style tags before layout effects read layout",
        explanation: "`useInsertionEffect` runs before DOM mutations occur, allowing CSS-in-JS libraries like Emotion/styled-components to inject `<style>` tags before `useLayoutEffect` measures geometry.",
      },
      {
        question: "Why does React Fiber use a singly linked tree with `child`, `sibling`, and `return` pointers?",
        options: [
          "To allow depth-first tree traversal that can be paused, yielded to the main thread, and resumed without relying on the JavaScript call stack",
          "To reduce JSON bundle size",
          "Because JavaScript engines do not support arrays of children",
          "To enable WebGL acceleration",
        ],
        correctAnswer: "To allow depth-first tree traversal that can be paused, yielded to the main thread, and resumed without relying on the JavaScript call stack",
        explanation: "Traditional recursive virtual DOM traversal cannot be paused without losing stack state. Fiber's linked list representation turns reconciliation into a resumable loop.",
      },
      {
        question: "What happens when a component returns `false` or `null` in JSX?",
        options: [
          "React renders nothing to the DOM for that component",
          "It throws a NullPointerException",
          "It renders the text 'false' or 'null' as strings",
          "It unmounts the entire parent component",
        ],
        correctAnswer: "React renders nothing to the DOM for that component",
        explanation: "In React, returning `null`, `undefined`, or booleans (`true`/`false`) causes React to render nothing in that component's position.",
      },
      {
        question: "What is the significance of the `lane` priority model in React 18?",
        options: [
          "A 31-bit bitmask priority system representing different tasks (urgent user input, transitions, idle work) enabling granular task scheduling",
          "A CSS Grid layout lane system",
          "A database connection pool lane",
          "A network throttling simulator in DevTools",
        ],
        correctAnswer: "A 31-bit bitmask priority system representing different tasks (urgent user input, transitions, idle work) enabling granular task scheduling",
        explanation: "Lanes use 32-bit integers as bitmasks to represent update priority and batching, allowing React to interleave, prioritize, and discard tasks flexibly.",
      },
      {
        question: "What does the `unstable_batchedUpdates` utility function from `react-dom` do in React 17 and earlier?",
        codeSnippet: "unstable_batchedUpdates(() => {\n  setCount(1);\n  setName('Alex');\n});",
        language: "javascript",
        options: [
          "Manually forced state updates inside async callbacks or timeouts to be batched into a single render pass",
          "Disabled batching completely",
          "Batched network fetch requests",
          "Converted class components to functional components",
        ],
        correctAnswer: "Manually forced state updates inside async callbacks or timeouts to be batched into a single render pass",
        explanation: "In React 17 and earlier, updates inside asynchronous contexts were not automatically batched. `unstable_batchedUpdates` provided an explicit opt-in wrapper.",
      },
    ],
  },
  {
    title: "React - Advanced Mastery & Gymnastics",
    description: "Concurrent rendering algorithms, custom reconcilers, Server Components, streaming SSR, and Hydration.",
    difficulty: "very hard",
    tags: ["React", "Frontend"],
    timeLimitMinutes: 25,
    questions: [
      {
        question: "What is the primary difference between React Server Components (RSC) and standard Client Components?",
        options: [
          "RSCs execute strictly on the server, have zero client JavaScript bundle footprint, and cannot use state or browser DOM APIs",
          "RSCs are rendered as static images",
          "Client Components cannot make network requests",
          "RSCs are only compatible with PHP backends",
        ],
        correctAnswer: "RSCs execute strictly on the server, have zero client JavaScript bundle footprint, and cannot use state or browser DOM APIs",
        explanation: "RSCs execute on the server and stream a serialized component tree to the client. Their dependencies are not included in the client JS bundle, drastically shrinking bundle size.",
      },
      {
        question: "What serialized format do React Server Components output to the browser?",
        options: [
          "A JSON-like stream of UI descriptions and module references (the RSC Wire Format), not raw HTML",
          "Pure binary WebAssembly byte arrays",
          "Plain HTML files with inline scripts",
          "Base64 encoded PNG screenshots",
        ],
        correctAnswer: "A JSON-like stream of UI descriptions and module references (the RSC Wire Format), not raw HTML",
        explanation: "RSC streams a special wire format representing the virtual component tree and client component boundary references, allowing client state to be preserved across server refetches.",
      },
      {
        question: "What causes a React Hydration Mismatch error?",
        codeSnippet: "export default function UserTime() {\n  return <span>{new Date().toLocaleTimeString()}</span>;\n}",
        language: "javascript",
        options: [
          "The server-rendered HTML string differs from the initial virtual DOM tree computed by the client during hydration",
          "The client browser is running in offline mode",
          "The CSS stylesheet failed to load",
          "A React version mismatch between client and server",
        ],
        correctAnswer: "The server-rendered HTML string differs from the initial virtual DOM tree computed by the client during hydration",
        explanation: "Hydration expects the client's initial render tree to match the server HTML byte-for-byte. Dynamic values (timestamps, random numbers, `window.innerWidth`) produce mismatches.",
      },
      {
        question: "How does `renderToPipeableStream` in React 18 improve server-side rendering over `renderToString`?",
        codeSnippet: "const { pipe } = renderToPipeableStream(<App />, {\n  bootstrapScripts: ['/main.js'],\n  onShellReady() { res.setHeader('content-type', 'text/html'); pipe(res); }\n});",
        language: "javascript",
        options: [
          "Streams the initial HTML shell immediately and progressively streams Suspense-wrapped content chunks as promises resolve without blocking",
          "Compresses the HTML into a zip file on the fly",
          "Caches the entire app in Redis",
          "Disables hydration on mobile devices",
        ],
        correctAnswer: "Streams the initial HTML shell immediately and progressively streams Suspense-wrapped content chunks as promises resolve without blocking",
        explanation: "`renderToPipeableStream` streams HTML progressively. If a section is wrapped in `<Suspense>`, React sends a fallback and streams the resolved HTML and replacement script later.",
      },
      {
        question: "What is Selective Hydration in React 18?",
        options: [
          "React can start hydrating components wrapped in `<Suspense>` independently before the entire HTML has finished streaming",
          "Hydrating only mobile users",
          "Hydrating components in alphabetical order",
          "Skipping hydration for authenticated users",
        ],
        correctAnswer: "React can start hydrating components wrapped in `<Suspense>` independently before the entire HTML has finished streaming",
        explanation: "Selective Hydration breaks the app into independent chunks via `<Suspense>`. Unrelated components can hydrate and become interactive even while other sections are still loading.",
      },
      {
        question: "What happens if a user clicks an unhydrated component during Selective Hydration in React 18?",
        options: [
          "React prioritizes hydrating that clicked component subtree first, replaying the user click once hydration finishes",
          "The click is discarded and ignored",
          "The browser page reloads",
          "A fatal JavaScript error is thrown",
        ],
        correctAnswer: "React prioritizes hydrating that clicked component subtree first, replaying the user click once hydration finishes",
        explanation: "React captures the user interaction and prioritizes hydrating that specific boundary ahead of others, then replays the event seamlessly.",
      },
      {
        question: "What does the `react-reconciler` package allow developers to build?",
        options: [
          "Custom renderers that target non-DOM environments like React Native, Three.js (React Three Fiber), terminal CLIs (Ink), or PDF generators",
          "Faster Babel compilers",
          "Custom browsers",
          "WebSocket proxy servers",
        ],
        correctAnswer: "Custom renderers that target non-DOM environments like React Native, Three.js (React Three Fiber), terminal CLIs (Ink), or PDF generators",
        explanation: "`react-reconciler` exposes the core Fiber reconciliation engine, allowing you to define host config methods (`createInstance`, `appendInitialChild`) for any custom platform.",
      },
      {
        question: "How does React manage work expiration and prevent high-priority updates from starving lower-priority work indefinitely?",
        options: [
          "Each update has an expiration timestamp; if lower-priority work is starved past its expiration, it is elevated to urgent status and executed synchronously",
          "Starved work is automatically deleted",
          "React reboots the web worker thread",
          "The browser drops frames intentionally",
        ],
        correctAnswer: "Each update has an expiration timestamp; if lower-priority work is starved past its expiration, it is elevated to urgent status and executed synchronously",
        explanation: "To prevent starvation, React assigns an expiration time to updates. Once that deadline passes, the task becomes expired and forces synchronous execution.",
      },
      {
        question: "What is the purpose of the `suppressHydrationWarning` prop on a DOM element?",
        codeSnippet: "<span suppressHydrationWarning>{new Date().getFullYear()}</span>",
        language: "javascript",
        options: [
          "Silences React's console mismatch warning when text content or attributes intentionally differ between server and client",
          "Disables client-side JavaScript execution for that tag",
          "Converts the tag to an image",
          "Forces the server to re-render the element",
        ],
        correctAnswer: "Silences React's console mismatch warning when text content or attributes intentionally differ between server and client",
        explanation: "`suppressHydrationWarning={true}` tells React not to warn if the text or attributes on that specific single element differ between SSR and client.",
      },
      {
        question: "What is the relationship between the `useOptimistic` hook in React 19 and Server Actions?",
        codeSnippet: "const [optimisticLikes, addOptimisticLike] = useOptimistic(\n  likes,\n  (state, update) => state + 1\n);",
        language: "javascript",
        options: [
          "Instantly updates the UI with an optimistic value while an asynchronous Server Action is in flight, automatically reverting if the action fails",
          "Caches the action response in IndexedDB",
          "Retries the network request 5 times",
          "Validates form inputs with regular expressions",
        ],
        correctAnswer: "Instantly updates the UI with an optimistic value while an asynchronous Server Action is in flight, automatically reverting if the action fails",
        explanation: "`useOptimistic` allows components to immediately show anticipated changes during async transitions, automatically resetting back to server state once the action finishes.",
      },
      {
        question: "What does the `useFormStatus` hook in React 19 provide?",
        codeSnippet: "function SubmitButton() {\n  const { pending, data, method } = useFormStatus();\n  return <button disabled={pending}>{pending ? 'Saving...' : 'Save'}</button>;\n}",
        language: "javascript",
        options: [
          "Status information (such as `pending`) of the parent `<form>` without having to pass props down through intermediate components",
          "Validates form input schemas using Zod",
          "Saves form inputs to localStorage",
          "Encodes form fields to base64",
        ],
        correctAnswer: "Status information (such as `pending`) of the parent `<form>` without having to pass props down through intermediate components",
        explanation: "`useFormStatus` is called from inside form children to read whether the parent `<form>` is currently submitting.",
      },
      {
        question: "What does the `useActionState` hook (formerly `useFormState`) do in React 19?",
        codeSnippet: "const [state, formAction, isPending] = useActionState(updateName, initialState);",
        language: "javascript",
        options: [
          "Manages state updated by an async action, returning the current state, an action wrapper, and a pending boolean",
          "Creates a Redux slice",
          "Encrypts action payloads",
          "Bypasses CORS restrictions",
        ],
        correctAnswer: "Manages state updated by an async action, returning the current state, an action wrapper, and a pending boolean",
        explanation: "`useActionState(fn, initialState)` binds an asynchronous action function to component state, tracking pending status and capturing return values.",
      },
      {
        question: "What does the `use()` hook do when passed a Promise in React 19?",
        codeSnippet: "function UserProfile({ userPromise }) {\n  const user = use(userPromise);\n  return <h1>{user.name}</h1>;\n}",
        language: "javascript",
        options: [
          "Reads the value of the promise synchronously, suspending the component until the promise resolves",
          "Creates an async generator function",
          "Converts the promise into an observable",
          "Rejects the promise if it takes longer than 1 second",
        ],
        correctAnswer: "Reads the value of the promise synchronously, suspending the component until the promise resolves",
        explanation: "The `use()` hook unwraps promises inside render, integrating directly with `<Suspense>` and allowing conditional calls unlike traditional React hooks.",
      },
      {
        question: "What is the memory footprint difference between retaining objects in a closure vs holding them in a `useRef`?",
        options: [
          "Refs hold objects as mutable properties on a Fiber node; closures retain outer lexical scopes in memory until the function reference is garbage collected",
          "Refs are stored in browser localStorage",
          "Closures use GPU memory",
          "There is no difference in heap memory allocation",
        ],
        correctAnswer: "Refs hold objects as mutable properties on a Fiber node; closures retain outer lexical scopes in memory until the function reference is garbage collected",
        explanation: "Closures can inadvertently capture entire surrounding scope scopes. A ref holds a direct single object reference that can be cleared by setting `.current = null`.",
      },
      {
        question: "How does React avoid race conditions when multiple overlapping transitions are triggered via `useTransition`?",
        options: [
          "React discards stale in-flight work and only commits the results of the latest transition update",
          "React executes them sequentially with a 500ms debounce",
          "React throws a concurrent exception",
          "React merges the state objects using deep merge",
        ],
        correctAnswer: "React discards stale in-flight work and only commits the results of the latest transition update",
        explanation: "When a newer transition update arrives, React abandons the stale work-in-progress tree for the older transition, only rendering the most recent update.",
      },
      {
        question: "What happens when you pass a Server Component as a `prop` (or `children`) to a Client Component?",
        codeSnippet: "// In Server Component:\n<ClientSidebar>\n  <ServerFeed /> {/* Passed as children */}\n</ClientSidebar>",
        language: "javascript",
        options: [
          "`ServerFeed` executes on the server, and its pre-rendered output is passed into `ClientSidebar` without becoming a client bundle dependency",
          "`ServerFeed` is converted to a Client Component automatically",
          "It throws an 'Invalid Server Component Nesting' exception",
          "The client cannot render the children",
        ],
        correctAnswer: "`ServerFeed` executes on the server, and its pre-rendered output is passed into `ClientSidebar` without becoming a client bundle dependency",
        explanation: "Nesting Server Components as `children` of Client Components preserves their server-only execution: the server evaluates the child, and passes the rendered elements to the client wrapper.",
      },
      {
        question: "Why is `dangerouslySetInnerHTML` prepended with the word 'dangerously'?",
        codeSnippet: "<div dangerouslySetInnerHTML={{ __html: rawHtmlString }} />",
        language: "javascript",
        options: [
          "To remind developers that injecting unescaped raw HTML exposes the application to Cross-Site Scripting (XSS) vulnerabilities",
          "Because it slows down DOM rendering by 90%",
          "Because it disables CSS styling",
          "To prevent minifiers from removing the attribute",
        ],
        correctAnswer: "To remind developers that injecting unescaped raw HTML exposes the application to Cross-Site Scripting (XSS) vulnerabilities",
        explanation: "Directly setting `innerHTML` bypasses React's automatic string escaping, which can allow malicious scripts in untrusted user input to execute on clients.",
      },
      {
        question: "What does the `activity` or `offscreen` component in experimental React concurrent features enable?",
        codeSnippet: "<Activity mode={tab === 'chat' ? 'visible' : 'hidden'}>\n  <ChatTab />\n</Activity>",
        language: "javascript",
        options: [
          "Preserves the component's state and DOM nodes while deprioritizing its updates and hiding it from the screen without unmounting",
          "Destroys the component to save memory",
          "Renders the component on a remote server",
          "Converts the component to a web worker",
        ],
        correctAnswer: "Preserves the component's state and DOM nodes while deprioritizing its updates and hiding it from the screen without unmounting",
        explanation: "The `Activity` (formerly Offscreen) API lets React preserve state and DOM trees for inactive tabs, deferring background updates and eliminating remount costs.",
      },
      {
        question: "What does `React.Children.only(children)` enforce?",
        codeSnippet: "React.Children.only(this.props.children);",
        language: "javascript",
        options: [
          "Verifies that `children` has exactly one child element and returns it, throwing an error otherwise",
          "Converts all children into a single string",
          "Filters out all elements except the first",
          "Disables child component re-renders",
        ],
        correctAnswer: "Verifies that `children` has exactly one child element and returns it, throwing an error otherwise",
        explanation: "`React.Children.only()` asserts that the component received precisely one child React element, throwing an exception if passed zero or multiple children.",
      },
      {
        question: "How does the React Fiber reconciler differentiate between updating an existing DOM node versus re-creating it from scratch?",
        options: [
          "It compares the element's `key` and `type`: if both match the previous Fiber, it updates the existing node; otherwise it destroys and remounts",
          "It compares the inner HTML text content",
          "It checks the element's CSS class names",
          "It checks if the component file path has changed",
        ],
        correctAnswer: "It compares the element's `key` and `type`: if both match the previous Fiber, it updates the existing node; otherwise it destroys and remounts",
        explanation: "During reconciliation diffing, if both `key` and `type` match between the current and new element, React reuses the existing Fiber and DOM node. If either differs, it destroys and rebuilds.",
      },
    ],
  },
];
