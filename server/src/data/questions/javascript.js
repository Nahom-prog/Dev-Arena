import { javascriptMidQuizzes } from "./javascript/mid.js";

/**
 * JavaScript Question Pack
 * 4 Difficulties: Easy, Mid (20 Topic Quizzes), Hard, Very Hard
 */

export const javascriptChallenges = [
  {
    title: "JavaScript - Practical Fundamentals",
    description: "ES6 variable declarations, template strings, fundamental array methods, primitives, and boolean truthiness.",
    difficulty: "easy",
    tags: ["JavaScript", "Frontend"],
    timeLimitMinutes: 15,
    questions: [
      {
        question: "What is the key scoping difference between `let` and `var` in JavaScript?",
        codeSnippet: "if (true) {\n  var a = 1;\n  let b = 2;\n}\nconsole.log(a); // 1\nconsole.log(b); // ReferenceError",
        language: "javascript",
        options: [
          "`let` is block-scoped; `var` is function-scoped (or globally scoped)",
          "`var` cannot be reassigned; `let` can",
          "`let` is hoisted to the top of the function; `var` is not",
          "`let` values are automatically frozen",
        ],
        correctAnswer: "`let` is block-scoped; `var` is function-scoped (or globally scoped)",
        explanation: "`let` and `const` adhere to block scoping `{ ... }`. `var` is scoped to the enclosing function regardless of block boundaries.",
      },
      {
        question: "What will the following template literal snippet output?",
        codeSnippet: "const user = 'Nahom';\nconst role = 'Engineer';\nconsole.log(`${user} is an ${role}`);",
        language: "javascript",
        options: [
          "${user} is an ${role}",
          "Nahom is an Engineer",
          "'Nahom' + ' is an ' + 'Engineer'",
          "undefined is an undefined",
        ],
        correctAnswer: "Nahom is an Engineer",
        explanation: "Template literals (enclosed by backticks `` ` ``) interpolate variables inside `${expression}` placeholders directly into the string.",
      },
      {
        question: "Which array method returns the first element that satisfies the provided testing function?",
        codeSnippet: "const users = [{ id: 1, name: 'A' }, { id: 2, name: 'B' }];\nconst found = users.find(u => u.id === 2);",
        language: "javascript",
        options: [
          "users.filter()",
          "users.indexOf()",
          "users.find()",
          "users.includes()",
        ],
        correctAnswer: "users.find()",
        explanation: "`.find()` returns the value of the first element in the array that satisfies the provided testing callback function. If no elements satisfy it, `undefined` is returned.",
      },
      {
        question: "What does the following destructuring assignment log to the console?",
        codeSnippet: "const colors = ['red', 'green', 'blue'];\nconst [, secondColor] = colors;\nconsole.log(secondColor);",
        language: "javascript",
        options: [
          "'red'",
          "undefined",
          "['green', 'blue']",
          "'green'",
        ],
        correctAnswer: "'green'",
        explanation: "In array destructuring, an empty comma `,` skips the element at that index, so `secondColor` receives the item at index 1 (`'green'`).",
      },
      {
        question: "What is the result of `Boolean('')`, `Boolean(0)`, and `Boolean(NaN)` in JavaScript?",
        options: [
          "All three evaluate to `false`",
          "All three evaluate to `true`",
          "`true`, `false`, `false`",
          "`false`, `false`, `true`",
        ],
        correctAnswer: "All three evaluate to `false`",
        explanation: "The empty string `''`, number `0`, `-0`, `0n`, `null`, `undefined`, and `NaN` are all strictly falsy values in JavaScript.",
      },
      {
        question: "What will `nums.length` be after executing this code?",
        codeSnippet: "const nums = [10, 20, 30];\nnums.length = 1;\nconsole.log(nums);",
        language: "javascript",
        options: [
          "3 (length property on arrays is read-only)",
          "[10] (setting length truncates the array)",
          "TypeError: Cannot assign to read only property",
          "[10, undefined, undefined]",
        ],
        correctAnswer: "[10] (setting length truncates the array)",
        explanation: "The `.length` property on JavaScript arrays is writable. Reducing it truncates the array permanently.",
      },
      {
        question: "Which operator provides default values only when the left operand is `null` or `undefined` (nullish coalescing)?",
        codeSnippet: "const score = 0;\nconst displayScore = score ?? 10;\nconsole.log(displayScore);",
        language: "javascript",
        options: [
          "||",
          "&&",
          "??",
          "?:",
        ],
        correctAnswer: "??",
        explanation: "The nullish coalescing operator `??` returns its right-hand operand only when its left-hand operand is `null` or `undefined`. Unlike `||`, it does not treat `0` or `''` as missing.",
      },
      {
        question: "What does `Array.prototype.includes()` return if an element exists inside the array?",
        codeSnippet: "const fruits = ['apple', 'orange', 'banana'];\nconsole.log(fruits.includes('orange'));",
        language: "javascript",
        options: [
          "1 (the index of the element)",
          "true",
          "'orange'",
          "['orange']",
        ],
        correctAnswer: "true",
        explanation: "`.includes()` returns a boolean (`true` if found, `false` otherwise).",
      },
      {
        question: "What will the following code output?",
        codeSnippet: "const user = { name: 'Sarah', age: 24 };\nconst { name: fullName } = user;\nconsole.log(fullName);",
        language: "javascript",
        options: [
          "undefined",
          "'Sarah'",
          "{ fullName: 'Sarah' }",
          "ReferenceError: fullName is not defined",
        ],
        correctAnswer: "'Sarah'",
        explanation: "The syntax `{ name: fullName }` unpacks the property `name` from `user` and assigns its value to a new variable called `fullName`.",
      },
      {
        question: "What does the spread operator `...` do when used inside an object literal?",
        codeSnippet: "const base = { a: 1, b: 2 };\nconst copy = { ...base, c: 3 };",
        language: "javascript",
        options: [
          "Deep clones `base` including functions and prototypes",
          "Freezes `base` to prevent further changes",
          "Copies own enumerable properties from `base` into the new object",
          "Deletes properties `a` and `b` from `base`",
        ],
        correctAnswer: "Copies own enumerable properties from `base` into the new object",
        explanation: "The spread operator copies own enumerable properties from one object into a new object literal (shallow copy).",
      },
      {
        question: "What is the return value of `typeof NaN` in JavaScript?",
        options: [
          "'nan'",
          "'undefined'",
          "'object'",
          "'number'",
        ],
        correctAnswer: "'number'",
        explanation: "In JavaScript, `NaN` stands for 'Not-a-Number', but its data type according to the IEEE-754 floating-point specification is `number`.",
      },
      {
        question: "What will `console.log(1 + '2' + 3)` output?",
        codeSnippet: "console.log(1 + '2' + 3);",
        language: "javascript",
        options: [
          "6",
          "'123'",
          "'33'",
          "NaN",
        ],
        correctAnswer: "'123'",
        explanation: "The `+` operator evaluates left-to-right. `1 + '2'` coerces 1 to string yielding `'12'`. Then `'12' + 3` yields `'123'`.",
      },
      {
        question: "Which method converts a JavaScript object into a JSON formatted string?",
        codeSnippet: "const obj = { id: 10 };\nconst str = JSON.stringify(obj);",
        language: "javascript",
        options: [
          "JSON.parse()",
          "JSON.stringify()",
          "Object.toJSON()",
          "String(obj)",
        ],
        correctAnswer: "JSON.stringify()",
        explanation: "`JSON.stringify()` converts a JavaScript object or value to a valid JSON string. `JSON.parse()` does the reverse.",
      },
      {
        question: "What will `console.log(Array.isArray({}))` output?",
        options: [
          "false",
          "true",
          "undefined",
          "TypeError",
        ],
        correctAnswer: "false",
        explanation: "`Array.isArray()` returns `true` only if the passed argument is an actual Array. Regular objects return `false`.",
      },
      {
        question: "What does the `Array.prototype.slice()` method do when called with `arr.slice(1, 3)`?",
        codeSnippet: "const letters = ['a', 'b', 'c', 'd', 'e'];\nconst res = letters.slice(1, 3);",
        language: "javascript",
        options: [
          "Mutates `letters` by removing elements at index 1 and 2",
          "Returns a shallow copy containing elements from index 1 up to (but not including) index 3: `['b', 'c']`",
          "Returns elements from index 1 through index 3 inclusive: `['b', 'c', 'd']`",
          "Splits the array into two halves",
        ],
        correctAnswer: "Returns a shallow copy containing elements from index 1 up to (but not including) index 3: `['b', 'c']`",
        explanation: "`slice(start, end)` extracts up to but not including `end` without modifying the original array.",
      },
      {
        question: "What is the output of `console.log([1, 2] == [1, 2])`?",
        options: [
          "true",
          "undefined",
          "false",
          "TypeError",
        ],
        correctAnswer: "false",
        explanation: "In JavaScript, objects and arrays are compared by memory reference, not by content. Two independently allocated array literals occupy distinct memory addresses.",
      },
      {
        question: "What does `Object.keys({ a: 1, b: 2 })` return?",
        options: [
          "['a', 'b']",
          "[1, 2]",
          "[['a', 1], ['b', 2]]",
          "'a, b'",
        ],
        correctAnswer: "['a', 'b']",
        explanation: "`Object.keys()` returns an array of a given object's own enumerable string property names.",
      },
      {
        question: "How do you check if a property exists as an own (non-inherited) property on an object in modern JavaScript?",
        codeSnippet: "const user = { name: 'Alex' };\nconsole.log(Object.hasOwn(user, 'name'));",
        language: "javascript",
        options: [
          "Object.hasOwn(user, 'name')",
          "user.isProperty('name')",
          "user.contains('name')",
          "Object.exists(user, 'name')",
        ],
        correctAnswer: "Object.hasOwn(user, 'name')",
        explanation: "`Object.hasOwn(obj, prop)` is the modern, safe ECMAScript 2022 replacement for `obj.hasOwnProperty(prop)` that works even on `Object.create(null)` objects.",
      },
      {
        question: "What will `console.log(Math.max())` output in JavaScript?",
        options: [
          "0",
          "NaN",
          "Infinity",
          "-Infinity",
        ],
        correctAnswer: "-Infinity",
        explanation: "`Math.max()` with no arguments returns `-Infinity` as the baseline comparison identity for finding the highest number.",
      },
      {
        question: "What does `Array.prototype.reduce()` return if given an array of numbers and an initial value of `0`?",
        codeSnippet: "const sum = [1, 2, 3, 4].reduce((acc, curr) => acc + curr, 0);",
        language: "javascript",
        options: [
          "[1, 3, 6, 10]",
          "10",
          "undefined",
          "0",
        ],
        correctAnswer: "10",
        explanation: "`.reduce()` executes a reducer function on each element, accumulating the results into a single output value (`0 + 1 + 2 + 3 + 4 = 10`).",
      },
    ],
  },
  ...javascriptMidQuizzes,
  {
    title: "JavaScript - Deep Mechanics & Internals",
    description: "Event loop call stacks, microtask draining, prototype chains, memory leaks, and generator iterables.",
    difficulty: "hard",
    tags: ["JavaScript", "Frontend"],
    timeLimitMinutes: 20,
    questions: [
      {
        question: "What is the exact execution order of the console logs below?",
        codeSnippet: "console.log('A');\nsetTimeout(() => console.log('B'), 0);\nPromise.resolve().then(() => console.log('C'));\nconsole.log('D');",
        language: "javascript",
        options: [
          "'A', 'B', 'C', 'D'",
          "'A', 'D', 'C', 'B'",
          "'A', 'D', 'B', 'C'",
          "'A', 'C', 'D', 'B'",
        ],
        correctAnswer: "'A', 'D', 'C', 'B'",
        explanation: "Synchronous logs run first ('A', 'D'). Then the microtask queue (Promise.then) is completely drained ('C'). Then the macrotask queue (setTimeout callback) executes ('B').",
      },
      {
        question: "How does property lookup work along the prototype chain?",
        codeSnippet: "function Person(name) { this.name = name; }\nPerson.prototype.sayHi = function() { return `Hi ${this.name}`; };\nconst p = new Person('Nahom');",
        language: "javascript",
        options: [
          "If a property is not found on `p`, the engine checks `p.__proto__` (`Person.prototype`), and continues up until `Object.prototype`, then `null`",
          "Properties are copied to `p` at instantiation time",
          "The engine queries a global symbol registry table",
          "Properties must be defined on the constructor function itself",
        ],
        correctAnswer: "If a property is not found on `p`, the engine checks `p.__proto__` (`Person.prototype`), and continues up until `Object.prototype`, then `null`",
        explanation: "JavaScript traverses `[[Prototype]]` references step by step until the property is found or `null` is reached, returning `undefined`.",
      },
      {
        question: "What is the key characteristic of `WeakMap` compared to standard `Map`?",
        codeSnippet: "const wm = new WeakMap();\nlet obj = { id: 1 };\nwm.set(obj, 'metadata');\nobj = null; // What happens to the metadata in wm?",
        language: "javascript",
        options: [
          "Keys must be objects (or non-registered symbols) and are held weakly: if no other references exist, the entry can be garbage collected",
          "WeakMap values are converted to strings",
          "WeakMap is iterable with `.forEach()`",
          "WeakMap stores data permanently in localStorage",
        ],
        correctAnswer: "Keys must be objects (or non-registered symbols) and are held weakly: if no other references exist, the entry can be garbage collected",
        explanation: "In `WeakMap`, keys are weakly held references. When the key object is unreferenced elsewhere, the garbage collector can clean up both key and value, preventing memory leaks.",
      },
      {
        question: "What does the `yield*` expression do inside a JavaScript generator function?",
        codeSnippet: "function* inner() { yield 1; yield 2; }\nfunction* outer() {\n  yield* inner();\n  yield 3;\n}",
        language: "javascript",
        options: [
          "Delegates iteration to another iterable or generator function sequentially",
          "Multiplies the yielded value by the next argument",
          "Terminates the generator immediately",
          "Converts the generator to a Promise",
        ],
        correctAnswer: "Delegates iteration to another iterable or generator function sequentially",
        explanation: "`yield*` delegates iteration to another generator or iterable, yielding each of its values in order before continuing.",
      },
      {
        question: "Which common programming pattern creates an unintended memory leak in single-page JavaScript applications?",
        codeSnippet: "window.addEventListener('resize', onResize);\n// Component unmounts without calling removeEventListener",
        language: "javascript",
        options: [
          "Dangling event listeners registered on `window` or `document` that retain references to unmounted component closures",
          "Using `const` instead of `let`",
          "Returning objects from functions",
          "Calling `JSON.parse()` repeatedly",
        ],
        correctAnswer: "Dangling event listeners registered on `window` or `document` that retain references to unmounted component closures",
        explanation: "Global listeners remain active on `window`. Because the listener callback references component variables, the garbage collector cannot free the component's memory.",
      },
      {
        question: "What will the following code output regarding the prototype of an arrow function?",
        codeSnippet: "const arrowFn = () => {};\nconsole.log(arrowFn.prototype);",
        language: "javascript",
        options: [
          "Object.prototype",
          "undefined",
          "{}",
          "TypeError: cannot access prototype",
        ],
        correctAnswer: "undefined",
        explanation: "Arrow functions cannot be used as constructors and therefore have no `.prototype` property (`undefined`). Calling `new arrowFn()` throws a TypeError.",
      },
      {
        question: "What does `Object.create(null)` produce?",
        codeSnippet: "const dict = Object.create(null);\nconsole.log(dict.toString);",
        language: "javascript",
        options: [
          "A pure dictionary object with no prototype chain (`dict.__proto__ === undefined`), free of inherited Object properties like `toString`",
          "A null pointer exception",
          "An empty array",
          "A frozen copy of Object.prototype",
        ],
        correctAnswer: "A pure dictionary object with no prototype chain (`dict.__proto__ === undefined`), free of inherited Object properties like `toString`",
        explanation: "`Object.create(null)` creates an object with a prototype of `null`. It inherits nothing, eliminating prototype pollution risks for dictionaries.",
      },
      {
        question: "What is the difference between `for..in` and `for..of` loops in JavaScript?",
        codeSnippet: "const arr = ['a', 'b', 'c'];\narr.customProp = 'extra';",
        language: "javascript",
        options: [
          "`for..in` iterates over all enumerable property keys (including prototype keys); `for..of` iterates over values of an iterable",
          "`for..in` is faster than `for..of`",
          "`for..of` only works on plain objects",
          "`for..in` cannot be used on arrays",
        ],
        correctAnswer: "`for..in` iterates over all enumerable property keys (including prototype keys); `for..of` iterates over values of an iterable",
        explanation: "`for..in` inspects keys/properties (including non-index keys and inherited properties). `for..of` calls the iterable's `[Symbol.iterator]` to retrieve elements.",
      },
      {
        question: "What is tail call optimization (TCO) in ES6 strict mode?",
        codeSnippet: "'use strict';\nfunction factorial(n, acc = 1) {\n  if (n <= 1) return acc;\n  return factorial(n - 1, n * acc); // Tail call\n}",
        language: "javascript",
        options: [
          "The engine can reuse the current stack frame for tail recursive calls, preventing stack overflow errors",
          "It forces the function to execute in reverse order",
          "It compiles recursive functions to loops at runtime in V8",
          "It restricts recursion to 10 iterations",
        ],
        correctAnswer: "The engine can reuse the current stack frame for tail recursive calls, preventing stack overflow errors",
        explanation: "A proper tail call occurs when the last action of a function is to call another function. TCO reuses the active stack frame instead of pushing a new one.",
      },
      {
        question: "What will `console.log(typeof (function() {})())` output?",
        codeSnippet: "const result = (function() {})();\nconsole.log(typeof result);",
        language: "javascript",
        options: [
          "'function'",
          "'undefined'",
          "'object'",
          "ReferenceError",
        ],
        correctAnswer: "'undefined'",
        explanation: "The IIFE (Immediately Invoked Function Expression) executes and returns nothing (`undefined`). Thus `typeof result` is `'undefined'`.",
      },
      {
        question: "What does `Reflect.ownKeys(obj)` return that `Object.keys(obj)` omits?",
        codeSnippet: "const sym = Symbol('secret');\nconst target = { regular: 1, [sym]: 2 };",
        language: "javascript",
        options: [
          "Symbol property keys and non-enumerable property keys",
          "Only methods on the prototype",
          "The object's constructor parameters",
          "Inherited properties from Object.prototype",
        ],
        correctAnswer: "Symbol property keys and non-enumerable property keys",
        explanation: "`Reflect.ownKeys()` returns all own property keys (both string and Symbol, whether enumerable or non-enumerable). `Object.keys()` only returns enumerable string keys.",
      },
      {
        question: "What is the purpose of `Symbol.iterator` on a custom JavaScript object?",
        codeSnippet: "const collection = {\n  items: [1, 2, 3],\n  [Symbol.iterator]() {\n    let idx = 0;\n    return {\n      next: () => ({ value: this.items[idx++], done: idx > this.items.length })\n    };\n  }\n};",
        language: "javascript",
        options: [
          "Implements the iterable protocol, allowing the object to be iterated with `for..of` loops and spread `[...collection]`",
          "Converts the object to an array in memory",
          "Freezes the items array",
          "Creates a database cursor",
        ],
        correctAnswer: "Implements the iterable protocol, allowing the object to be iterated with `for..of` loops and spread `[...collection]`",
        explanation: "Defining `[Symbol.iterator]` makes any custom object conform to the ECMAScript iteration protocols, enabling `for..of`, spread, and destructuring.",
      },
      {
        question: "What happens when you call `Promise.race([p1, p2])`?",
        codeSnippet: "const winner = await Promise.race([\n  fetch('/api/primary'),\n  new Promise((_, reject) => setTimeout(() => reject('Timeout'), 3000))\n]);",
        language: "javascript",
        options: [
          "Returns a promise that settles (fulfills or rejects) as soon as the first promise in the iterable settles",
          "Returns only if both promises succeed",
          "Cancels the slower promise's network request",
          "Executes the promises in parallel threads",
        ],
        correctAnswer: "Returns a promise that settles (fulfills or rejects) as soon as the first promise in the iterable settles",
        explanation: "`Promise.race()` takes the outcome (value or error) of whichever promise settles first, commonly used for request timeouts.",
      },
      {
        question: "Why does `JSON.stringify()` omit properties whose values are `undefined`, `Function`, or `Symbol`?",
        codeSnippet: "const data = { a: 1, b: undefined, c: () => {} };\nconsole.log(JSON.stringify(data));",
        language: "javascript",
        options: [
          "`'{\"a\":1}'` (these types have no valid representation in the JSON standard and are omitted from object serialization)",
          "`'{\"a\":1,\"b\":null,\"c\":null}'`",
          "TypeError: cannot serialize function",
          "`'{\"a\":1,\"b\":undefined}'`",
        ],
        correctAnswer: "`'{\"a\":1}'` (these types have no valid representation in the JSON standard and are omitted from object serialization)",
        explanation: "In JSON specification, `undefined`, functions, and symbols are not valid types. In object properties, `JSON.stringify()` drops them entirely.",
      },
      {
        question: "What does `Object.defineProperty(obj, 'prop', { writable: false, configurable: false })` enforce?",
        codeSnippet: "Object.defineProperty(user, 'id', {\n  value: 101,\n  writable: false,\n  configurable: false,\n});",
        language: "javascript",
        options: [
          "The property cannot be reassigned, deleted, or reconfigured with another descriptor",
          "The property cannot be read",
          "The property is hidden from all functions",
          "The property can only be accessed inside classes",
        ],
        correctAnswer: "The property cannot be reassigned, deleted, or reconfigured with another descriptor",
        explanation: "`writable: false` blocks value mutations. `configurable: false` prevents deleting the property or altering its property descriptor attributes.",
      },
      {
        question: "What will `console.log(Function.length)` and `function add(a, b = 1, c) {}.length` report?",
        codeSnippet: "function test(a, b = 1, c) {}\nconsole.log(test.length);",
        language: "javascript",
        options: [
          "1 (the length property of a function reports the number of parameters before the first default parameter)",
          "3 (total parameters count)",
          "2",
          "undefined",
        ],
        correctAnswer: "1 (the length property of a function reports the number of parameters before the first default parameter)",
        explanation: "A function's `.length` property specifies the number of arguments expected by the function, which stops counting at the first parameter with a default value.",
      },
      {
        question: "What is the result of `Object.is(-0, +0)` vs `-0 === +0` in JavaScript?",
        options: [
          "`Object.is(-0, +0)` is `false`; `-0 === +0` is `true`",
          "`Object.is(-0, +0)` is `true`; `-0 === +0` is `false`",
          "Both are `true`",
          "Both are `false`",
        ],
        correctAnswer: "`Object.is(-0, +0)` is `false`; `-0 === +0` is `true`",
        explanation: "`Object.is()` uses SameValue equality: it distinguishes `-0` from `+0`, and treats `NaN` as equal to `NaN`.",
      },
      {
        question: "What does `void 0` evaluate to in JavaScript, and why was it historically used?",
        codeSnippet: "const x = void 0;",
        language: "javascript",
        options: [
          "It safely evaluates to `undefined` because in older ECMAScript versions `undefined` was a mutable global property",
          "It returns `0` as an empty number",
          "It clears memory at pointer 0",
          "It stops script execution",
        ],
        correctAnswer: "It safely evaluates to `undefined` because in older ECMAScript versions `undefined` was a mutable global property",
        explanation: "The `void` operator evaluates an expression and returns `undefined`. `void 0` was popular in minifiers because it is shorter than `'undefined'` and cannot be overwritten.",
      },
      {
        question: "What happens if an asynchronous function throws an unhandled rejection inside `Promise.all()`?",
        codeSnippet: "Promise.all([fetch1(), fetch2WithError(), fetch3()]);",
        language: "javascript",
        options: [
          "The returned Promise immediately rejects with that error, ignoring any pending promises in the list",
          "The error is suppressed until all promises resolve",
          "It retries the failed promise once",
          "It returns an array with the error at that index",
        ],
        correctAnswer: "The returned Promise immediately rejects with that error, ignoring any pending promises in the list",
        explanation: "`Promise.all()` has fail-fast behavior: if any promise rejects, the entire returned promise rejects immediately with the reason of the first rejected promise.",
      },
      {
        question: "What is the difference between `new Array(3)` and `[3]` in JavaScript?",
        codeSnippet: "const a = new Array(3);\nconst b = [3];",
        language: "javascript",
        options: [
          "`new Array(3)` creates a sparse array with length 3 containing 3 empty slots; `[3]` creates an array with one element (the number 3)",
          "Both create an array containing `[3]`",
          "`new Array(3)` creates `[0, 1, 2]`",
          "`new Array(3)` throws a RangeError",
        ],
        correctAnswer: "`new Array(3)` creates a sparse array with length 3 containing 3 empty slots; `[3]` creates an array with one element (the number 3)",
        explanation: "When passed a single number argument, `new Array(n)` creates an array with length `n` whose slots are empty ('holes'). Literal `[3]` contains element 3.",
      },
    ],
  },
  {
    title: "JavaScript - Advanced Mastery & Gymnastics",
    description: "Proxy traps, Reflect API, Web Workers, TypedArrays, IEEE-754 bitwise quirks, and V8 optimization.",
    difficulty: "very hard",
    tags: ["JavaScript", "Frontend"],
    timeLimitMinutes: 25,
    questions: [
      {
        question: "What does the `Proxy` object in JavaScript allow developers to intercept?",
        codeSnippet: "const proxy = new Proxy(target, {\n  get(target, prop, receiver) {\n    return Reflect.get(target, prop, receiver);\n  }\n});",
        language: "javascript",
        options: [
          "Fundamental language operations on an object including property lookup, assignment, enumeration, and function invocation",
          "HTTP network requests before they leave the browser",
          "DNS lookups in the operating system",
          "Only array index assignments",
        ],
        correctAnswer: "Fundamental language operations on an object including property lookup, assignment, enumeration, and function invocation",
        explanation: "`Proxy` wraps a target object and intercepts internal operations through handler traps (e.g. `get`, `set`, `has`, `deleteProperty`, `apply`).",
      },
      {
        question: "Why should `Reflect.get(target, prop, receiver)` be used inside a Proxy getter trap instead of `target[prop]`?",
        codeSnippet: "const proxy = new Proxy(target, {\n  get(target, prop, receiver) {\n    return Reflect.get(target, prop, receiver); // Why receiver?\n  }\n});",
        language: "javascript",
        options: [
          "Passing `receiver` ensures that if `prop` is a getter on the prototype, `this` inside that getter correctly refers to the Proxy, not the raw target",
          "`target[prop]` throws a SyntaxError inside Proxies",
          "It forces the getter to return a Promise",
          "`Reflect.get()` automatically caches the property value",
        ],
        correctAnswer: "Passing `receiver` ensures that if `prop` is a getter on the prototype, `this` inside that getter correctly refers to the Proxy, not the raw target",
        explanation: "Passing the `receiver` (the proxy instance) preserves the correct `this` binding when getters access other properties on the proxy.",
      },
      {
        question: "What is the memory representation of `new ArrayBuffer(16)` in JavaScript?",
        codeSnippet: "const buffer = new ArrayBuffer(16);\nconst view = new Int32Array(buffer);",
        language: "javascript",
        options: [
          "A fixed-length raw binary data buffer of exactly 16 bytes",
          "An array of 16 JavaScript strings",
          "A compressed JSON file",
          "A circular linked list of 16 nodes",
        ],
        correctAnswer: "A fixed-length raw binary data buffer of exactly 16 bytes",
        explanation: "`ArrayBuffer` allocates a contiguous block of raw memory of the specified byte length that can be manipulated using TypedArray views (`Int32Array`, `Float64Array`, etc.).",
      },
      {
        question: "How many 32-bit integers can fit into an `ArrayBuffer` of 16 bytes?",
        codeSnippet: "const buffer = new ArrayBuffer(16);\nconst int32View = new Int32Array(buffer);\nconsole.log(int32View.length);",
        language: "javascript",
        options: [
          "16",
          "4 (each 32-bit integer occupies 4 bytes; 16 / 4 = 4)",
          "8",
          "32",
        ],
        correctAnswer: "4 (each 32-bit integer occupies 4 bytes; 16 / 4 = 4)",
        explanation: "32 bits = 4 bytes. An `ArrayBuffer` of 16 bytes accommodates exactly $16 / 4 = 4$ Int32 elements.",
      },
      {
        question: "What does the `FinalizationRegistry` API do in modern JavaScript?",
        codeSnippet: "const registry = new FinalizationRegistry(heldValue => {\n  console.log(`Cleaned up: ${heldValue}`);\n});\nregistry.register(targetObj, 'token-123');",
        language: "javascript",
        options: [
          "Requests a callback after an object registered with it has been garbage-collected",
          "Immediately forces the V8 engine to run full garbage collection",
          "Prevents an object from being garbage collected",
          "Serializes dead memory into disk swap space",
        ],
        correctAnswer: "Requests a callback after an object registered with it has been garbage-collected",
        explanation: "`FinalizationRegistry` lets you register cleanup callbacks that fire after a target object is reclaimed by the garbage collector.",
      },
      {
        question: "What happens when bitwise operators like `~`, `|`, or `^` are used on floating point numbers in JavaScript?",
        codeSnippet: "console.log(12.7 | 0);",
        language: "javascript",
        options: [
          "Operands are implicitly converted to 32-bit signed integers, truncating the fractional part to produce `12`",
          "It throws a TypeError because floats cannot be operated on bitwise",
          "It rounds to the nearest integer producing `13`",
          "It converts the number to scientific notation",
        ],
        correctAnswer: "Operands are implicitly converted to 32-bit signed integers, truncating the fractional part to produce `12`",
        explanation: "All JavaScript bitwise operators convert numbers to 32-bit signed integers using `ToInt32()`, effectively truncating any decimal fraction.",
      },
      {
        question: "What does the `Symbol.hasInstance` well-known symbol allow you to customize?",
        codeSnippet: "class EvenNumber {\n  static [Symbol.hasInstance](instance) {\n    return typeof instance === 'number' && instance % 2 === 0;\n  }\n}\nconsole.log(4 instanceof EvenNumber);",
        language: "javascript",
        options: [
          "Overrides the behavior of the `instanceof` operator for that class or constructor",
          "Creates a singleton instance of the class",
          "Checks if an object is stored on the heap or stack",
          "Prevents creating instances with `new`",
        ],
        correctAnswer: "Overrides the behavior of the `instanceof` operator for that class or constructor",
        explanation: "Methods named `[Symbol.hasInstance]` are invoked by the `instanceof` operator to determine whether a given value is recognized as an instance.",
      },
      {
        question: "What causes 'deoptimization' (bailout) in V8's TurboFan optimizing compiler?",
        codeSnippet: "function add(a, b) { return a + b; }\nadd(1, 2); // Monomorphic (integers)\nadd('x', 'y'); // Type mutation -> deopt!",
        language: "javascript",
        options: [
          "Passing arguments with varying types (megamorphic call sites) or mutating object hidden classes (shapes)",
          "Writing code with ES6 syntax",
          "Using template literals",
          "Having more than 3 parameters in a function",
        ],
        correctAnswer: "Passing arguments with varying types (megamorphic call sites) or mutating object hidden classes (shapes)",
        explanation: "V8 optimizes based on assumptions of stable types and hidden classes. When assumptions are violated (e.g. passing strings to an integer-optimized function), V8 deoptimizes back to interpreted bytecode.",
      },
      {
        question: "What is a SharedArrayBuffer and how does it differ from a standard ArrayBuffer?",
        codeSnippet: "const shared = new SharedArrayBuffer(1024);",
        language: "javascript",
        options: [
          "Its memory can be shared concurrently across multiple Web Workers without copying, requiring Atomics for synchronization",
          "It is stored in the cloud across browser sessions",
          "It automatically compresses JSON data",
          "It can only be used in Node.js server scripts",
        ],
        correctAnswer: "Its memory can be shared concurrently across multiple Web Workers without copying, requiring Atomics for synchronization",
        explanation: "`SharedArrayBuffer` creates a shared memory region accessible simultaneously by the main thread and Web Workers without message serialization overhead.",
      },
      {
        question: "What does `Atomics.wait()` do in JavaScript?",
        codeSnippet: "// In a dedicated Web Worker:\nAtomics.wait(int32Array, 0, 0);",
        language: "javascript",
        options: [
          "Blocks the worker thread until a specified condition is met or until woken up by `Atomics.notify()`",
          "Delays execution on the main UI thread",
          "Creates an atomic database transaction",
          "Checks if a variable is garbage-collected",
        ],
        correctAnswer: "Blocks the worker thread until a specified condition is met or until woken up by `Atomics.notify()`",
        explanation: "`Atomics.wait()` blocks execution in a Web Worker until the specified index in a SharedArrayBuffer changes or is notified via `Atomics.notify()`.",
      },
      {
        question: "What is the output of `console.log(typeof (1n))` in modern JavaScript?",
        codeSnippet: "const big = 1000000000000000000000000n;\nconsole.log(typeof big);",
        language: "javascript",
        options: [
          "'number'",
          "'bigint'",
          "'int64'",
          "'object'",
        ],
        correctAnswer: "'bigint'",
        explanation: "BigInt is a native primitive numeric type in JavaScript that can represent integers with arbitrary precision. Its `typeof` is `'bigint'`.",
      },
      {
        question: "What will `console.log(10n == 10)` vs `console.log(10n === 10)` output?",
        options: [
          "`true` for `==`, `false` for `===`",
          "`true` for both",
          "`false` for both",
          "`false` for `==`, `true` for `===`",
        ],
        correctAnswer: "`true` for `==`, `false` for `===`",
        explanation: "Loose equality `==` coerces between BigInt and Number if numeric values match (`10n == 10` is `true`). Strict equality `===` checks both value and type, returning `false`.",
      },
      {
        question: "What does the `Symbol.toPrimitive` method handle on an object?",
        codeSnippet: "const obj = {\n  [Symbol.toPrimitive](hint) {\n    if (hint === 'number') return 42;\n    return 'default';\n  }\n};\nconsole.log(+obj);",
        language: "javascript",
        options: [
          "Defines custom conversion logic when the object is coerced to a primitive value (`number`, `string`, or `default`)",
          "Serializes the object to an ArrayBuffer",
          "Converts the object to JSON format",
          "Overwrites the prototype of the object",
        ],
        correctAnswer: "Defines custom conversion logic when the object is coerced to a primitive value (`number`, `string`, or `default`)",
        explanation: "`[Symbol.toPrimitive](hint)` is called by JavaScript engines to convert an object to a corresponding primitive value depending on the context (`'number'`, `'string'`, `'default'`).",
      },
      {
        question: "What is an event loop 'macro-task' starvation?",
        codeSnippet: "function infiniteMicrotask() {\n  Promise.resolve().then(infiniteMicrotask);\n}\ninfiniteMicrotask();",
        language: "javascript",
        options: [
          "Continuously queuing microtasks prevents the event loop from ever reaching macrotasks (timers, I/O, UI rendering), freezing the webpage",
          "Running out of memory in the call stack",
          "A deadlock between two Web Workers",
          "When a setTimeout delay exceeds 24 days",
        ],
        correctAnswer: "Continuously queuing microtasks prevents the event loop from ever reaching macrotasks (timers, I/O, UI rendering), freezing the webpage",
        explanation: "Because microtasks are processed completely until the queue is empty before advancing to the next phase, an endlessly recurring microtask locks the thread and freezes rendering.",
      },
      {
        question: "What does `Object.preventExtensions(obj)` do?",
        codeSnippet: "const o = { x: 1 };\nObject.preventExtensions(o);\no.y = 2; // Silent failure in non-strict, TypeError in strict mode",
        language: "javascript",
        options: [
          "Prevents new properties from ever being added to the object",
          "Prevents prototype inheritance",
          "Makes all properties readonly",
          "Freezes all child objects",
        ],
        correctAnswer: "Prevents new properties from ever being added to the object",
        explanation: "`Object.preventExtensions()` marks the object as non-extensible so no new properties can ever be added to it. Existing properties can still be modified or deleted.",
      },
      {
        question: "What is the difference between `structuredClone()` and `JSON.parse(JSON.stringify())` regarding circular references?",
        codeSnippet: "const obj = {};\nobj.self = obj;",
        language: "javascript",
        options: [
          "`structuredClone()` handles circular references natively; `JSON.stringify()` throws a TypeError ('Converting circular structure to JSON')",
          "Both throw circular structure errors",
          "`JSON.stringify()` handles them; `structuredClone()` cannot",
          "`structuredClone()` converts circular references to `null`",
        ],
        correctAnswer: "`structuredClone()` handles circular references natively; `JSON.stringify()` throws a TypeError ('Converting circular structure to JSON')",
        explanation: "The HTML standard `structuredClone()` algorithm maintains an internal identity map of visited references, successfully duplicating cyclical data structures.",
      },
      {
        question: "How does the V8 engine represent small integers efficiently without allocating heap objects?",
        options: [
          "Using pointer tagging (Smi / Small Integer): encoding the integer directly in the pointer by setting the lowest bit to 0",
          "Storing numbers in browser cookies",
          "Compressing integers with gzip",
          "Converting integers to ASCII characters",
        ],
        correctAnswer: "Using pointer tagging (Smi / Small Integer): encoding the integer directly in the pointer by setting the lowest bit to 0",
        explanation: "V8 uses Smi (Small Integer) tagged pointers. Integers within 31 bits are stored directly inside the pointer register itself, bypassing heap allocation entirely.",
      },
      {
        question: "What does the `Intl.Segmenter` API provide in modern JavaScript?",
        codeSnippet: "const segmenter = new Intl.Segmenter('en', { granularity: 'word' });\nconst segments = segmenter.segment('Hello world!');",
        language: "javascript",
        options: [
          "Locale-sensitive text segmentation into graphemes, words, or sentences",
          "Memory segment pooling for WebAssembly",
          "URL pathname segment parsing",
          "Splitting strings by commas",
        ],
        correctAnswer: "Locale-sensitive text segmentation into graphemes, words, or sentences",
        explanation: "`Intl.Segmenter` enables locale-aware text splitting respecting emojis, compound characters, and language-specific word boundaries.",
      },
      {
        question: "What happens when you transfer an `ArrayBuffer` to a Web Worker via `postMessage(buffer, [buffer])`?",
        codeSnippet: "worker.postMessage({ buffer }, [buffer]);\nconsole.log(buffer.byteLength);",
        language: "javascript",
        options: [
          "Zero-copy transfer: memory ownership is moved to the worker, and the original buffer becomes detached (`byteLength === 0`)",
          "The buffer is cloned into a new 16KB chunk",
          "The main thread retains full read and write access",
          "A shared mutex lock is acquired",
        ],
        correctAnswer: "Zero-copy transfer: memory ownership is moved to the worker, and the original buffer becomes detached (`byteLength === 0`)",
        explanation: "Transferable objects undergo zero-copy ownership transfer. The sender's buffer is neutered/detached, instantly giving the receiver exclusive access without copying memory.",
      },
      {
        question: "What will `console.log(Math.fround(1.337))` do in JavaScript?",
        options: [
          "Returns the nearest 32-bit single precision float representation of a number",
          "Rounds down to the nearest integer",
          "Floats the number to exponential notation",
          "Returns a fraction string",
        ],
        correctAnswer: "Returns the nearest 32-bit single precision float representation of a number",
        explanation: "`Math.fround()` returns the nearest 32-bit single precision float representation, useful for WebGL and performance-critical numerical shaders.",
      },
    ],
  },
];
